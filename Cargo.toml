[package]
name = "usya"
version = "0.1.0"
edition = "2021"

[dependencies]
bootloader = "0.10"
tinybmp = "0.5.0"
embedded-graphics = "0.8.1"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
